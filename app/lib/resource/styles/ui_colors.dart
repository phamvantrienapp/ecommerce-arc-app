import 'package:flutter/widgets.dart';

class UIColors {
  UIColors._();

  // Image colors
  static const primaryImageColor = Color.fromRGBO(152, 238, 204, 1);

  // Button colors
  static const primaryButtonColor = Color.fromRGBO(69, 184, 233, 1);
  static const primaryTextButtonColor = Color.fromRGBO(255, 255, 255, 1);
  static const primaryButtonColor2 = Color.fromRGBO(253, 211, 225, 0.4);

  // Text colors
  static const primaryTextColor = Color.fromRGBO(70, 80, 93, 1);
  static const primaryTitleColor = Color.fromRGBO(41, 45, 50, 1);

  // Indicator colors
  static const indicatorColor = Color.fromRGBO(239, 242, 245, 1);
  static const indicatorActiveColor = Color.fromRGBO(234, 152, 91, 1);

  // Login and Registrion colors
  static const loginOptionColor = Color.fromRGBO(253, 211, 225, 0.5);
  static const loginOptionSelectedColor = Color.fromRGBO(244, 115, 158, 1);

  // App Text Field colors
  static const primaryTextFieldColor = Color.fromRGBO(239, 242, 245, 1);

  // App Text Box
  static const primaryTextBoxColor = Color.fromRGBO(239, 242, 245, 1);

  // Action button colors
  static const actionButtonColor = Color.fromRGBO(234, 152, 91, 1);
  static const actionButtonBackgroundColor = Color.fromRGBO(252, 226, 207, 1);

  // Common colors
  static const commonBlue = Color.fromRGBO(69, 184, 233, 1);
  static const commonDark = Color.fromRGBO(179, 191, 203, 1);
  static const commonGray = Color.fromRGBO(207, 215, 222, 1);
  static const commonPink = Color.fromRGBO(244, 115, 158, 1);
  static const commonBlack = Color.fromRGBO(41, 45, 50, 1);
  static const commonGreen = Color.fromRGBO(40, 185, 150, 1);
  static const commonPinkOP = Color.fromRGBO(253, 211, 225, 0.5);
  static const commonGrayWhite = Color.fromRGBO(239, 242, 245, 1);

  // Category colors
  static const categoryBackgroundColor = Color.fromRGBO(244, 115, 158, 0.12);

  // Home Menu Colors
  static const homeMenuColor = Color.fromRGBO(239, 242, 245, 1);
  static const homeMenuSelectedColor = Color.fromRGBO(0, 0, 0, 1);
  static const homeMenuUnselectedColor = Color.fromRGBO(27, 27, 27, 1);
}
