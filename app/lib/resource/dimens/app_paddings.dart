import 'package:flutter/material.dart';

class AppPaddings {
  AppPaddings._();

  static const paddingAll = EdgeInsets.all(12.0);
  static const paddingVertical = EdgeInsets.symmetric(vertical: 12.0);
  static const paddingHorizontal = EdgeInsets.symmetric(horizontal: 12.0);
}
